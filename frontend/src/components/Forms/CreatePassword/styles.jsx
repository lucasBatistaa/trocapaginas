import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({ 
    requirementsContainer: {
        gap: 10,
        marginVertical: 12,
    }, 

    requirementAlert: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 4,
    },
})