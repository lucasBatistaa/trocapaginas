import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({ 
    container: {
        marginVertical: 20,
    },

    requirementsContainer: {
        gap: 10,
        marginVertical: 12,
    }, 

    requirementAlert: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 4,
    },

    button: {
        alignItems: 'center',
        marginTop: 20,
    }
})